import 'package:flutter/material.dart';

class ResourceListPage extends StatelessWidget {
  const ResourceListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Resource List Page"));
  }
}
